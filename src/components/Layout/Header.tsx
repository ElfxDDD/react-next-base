import React from "react";

interface HeaderProps {}

const Header: React.FC<HeaderProps> = (props: HeaderProps) => {
    return <div className="w-full h-[64px]">HEADER</div>;
};

export default Header;
